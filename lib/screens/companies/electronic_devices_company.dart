import 'package:flutter/material.dart';
class Companies_screen extends StatelessWidget {
  const Companies_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}